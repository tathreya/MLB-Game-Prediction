# TODO: write this function to feature engineer (add features to Features table) for the current season 
# up until current day

# use same logic as historical features but only do it on games before current day so have a WHERE clause on the
# currentSchedule table