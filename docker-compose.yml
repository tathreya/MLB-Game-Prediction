services:
  mlb-app:
    build: .
    container_name: mlb-app
    ports:
      - "8001:8000"             # avoid Apple AirPlay conflicts
    volumes:
      - ./databases:/app/databases   # SQLite persistence
    env_file:
      - .env